syntax = "proto3";
package processor;
option go_package = "./;protos";

service Processor {
  rpc Process(ProcessQuery) returns (ProcessResult) {}
}

message ProcessQuery {
  repeated string labels = 1;
  bytes data = 2;
}

message ProcessResult {
  bool success = 1;
}